import { TrocarcorDirective } from './trocarcor.directive';

describe('TrocarcorDirective', () => {
  it('should create an instance', () => {
    const directive = new TrocarcorDirective();
    expect(directive).toBeTruthy();
  });
});
